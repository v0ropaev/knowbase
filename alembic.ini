# Alembic config. The database URL is resolved in migrations/env.py from the alembic config
# (set programmatically by kb.store.migrate) or the KB_DB_URL / KB_DB environment variables.
[alembic]
script_location = migrations
prepend_sys_path = src
